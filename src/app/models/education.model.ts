export interface Education {
    id?: number; 
    career: string;
    institution: string;
    img: string;
    url: string; 
    since: string;
    through: string;
    complete: boolean;
    description: string;
}